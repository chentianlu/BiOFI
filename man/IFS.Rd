% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IFS.R
\name{IFS}
\alias{IFS}
\title{IFS}
\usage{
IFS(
  microApath,
  metaApath,
  conf = NULL,
  groupInfo = NULL,
  nDFS = 0.25,
  nDS = 0.25,
  nES = 0.25,
  nAS = 0.25,
  NS = 2.5,
  r = 0.5,
  p_adjust = 0.05
)
}
\arguments{
\item{microApath}{A data frame including microbes and predicted KEGG pathways.}

\item{metaApath}{A data frame including metabolites with compound ID and predicted KEGG pathways from Meta2pathway function.}

\item{conf}{A data frame including confounders. Default value is NULL.}

\item{groupInfo}{A data frame including sample grouping information}

\item{nDFS}{A value indicated the weight of difference Score in the IFS formula. Default value is 0.25.}

\item{nDS}{A value indicated the weight of degree score in the IFS formula. Default value is 0.25.}

\item{nES}{A value indicated the weight of edge score in the IFS formula. Default value is 0.25.}

\item{nAS}{A value indicated the weight of abundance score in the IFS formula. Default value is 0.25.}

\item{NS}{A threshold value of IFS to identify the key nodes. Default value is 2.5.}

\item{r}{A threshold value of correlation coefficient to construct the network. Default value is 0.5.}

\item{p_adjust}{A threshold value of correlation adjust p value to construct the network. Default  value is 0.05.}
}
\value{
IFS_res A list including IFS and other sub-score results.
}
\description{
A function is used to calculates and identify the key nodes,the formula of IFS is:
              IFS= nDFS x DFS + nDS x DS + nES x ES + nAS x AS;
nDFS,nDS,nES,nAS indict the weight value of DFS,DS,ES and AS, and nDFS + nDS + nES + nAS = 1;
DFS means Difference Score;
DS means Degree Score;
ES means Edge Score;
AS means Abundance Score.
}
\examples{
IFScore <- IFS(microApath = micro.eg, metaApath = metabo.eg,
               conf = NULL, groupInfo = groupInfo.eg)
}
